describe('createStore', () => {
  it('should add redux devtools if they are available and the application is in dev mode', () => {

  });

  it('should not add redux devtools if they are not available', () => {

  });

  it('should not add redux devtools if the application is not in dev mode', () => {

  });
});
