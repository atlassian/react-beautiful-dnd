// @flow
it('should return null if combining is not enabled', () => {});

describe('moving forward', () => {
  it('should return null if there is nothing displaced', () => {});

  it('should combine with the closest displaced if moving forward', () => {});
});

describe('moving backwards', () => {
  describe('nothing is displaced', () => {
    it('should return null if the list has nothing in it', () => {});

    it('should combine with the last item in the list', () => {});
  });

  describe('there are items displaced', () => {
    it('should combine with the closest displaced item', () => {});
  });
});
