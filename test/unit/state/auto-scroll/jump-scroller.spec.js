// @flow

describe('jump scroller', () => {
  describe('window scrolling', () => {

  });

  describe('droppable scrolling', () => {
    it('should call "move" with the request if the draggable is too big', () => {

    });

    it('should scroll the droppable by the request if it can be done entirely', () => {

    });

    describe('playing with the window', () => {
      it('should ')
    });
  });


});