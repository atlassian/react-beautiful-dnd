// @flow
import type { DraggableDimension } from '../../types';
// This functions responsibility is to move a draggable above/below a draggable
type Args = {|
  source: DraggableDimension,
  destination: DraggableDimension,
|}

export default ({
  source,
  destination,
}: Args): Result => {

};
